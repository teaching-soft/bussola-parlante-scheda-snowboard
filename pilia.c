/* inserimento valore acquisito bruno pilia*/
# include <stdio.h>
# define max_posizioni 10
int posizione [max_posizioni];
void inserimento_val_acquisito(int v)
int main (void)
{
    int num[10],indice,i;
    for(i=0;i<21,i++)
    {
                     printf(" inserisci 10 valori\n");
                     scanf(" %d\n ", &val_acquisito);
                     }
    return 0;
}

void inserimento_val_acquisito(int val_acquisito)
{
     int static indice=0;
     if indice==max_posizioni
     {
                             indice=0;
                             }
     posizioni[indice]=val_acquisito
     indice ++;
     
     /* funzione che rimuove il valore più piccolo e più grande*/
void rimozione (void)
{
     int min,max,i,indice_min,indice_max;
     min=posizioni[0];
     max=posizioni[0];
     for(i=0;i<max_posizione;i++)
     }
if(posizioni[i]<min)
{
                    min=posizioni[i];
                    indice_min=i;
                    }
     if(posizioni[i];
     indice_max=i;
     }
posizioni[indice_min]=-1;
posizioni[indice_max]=-1;

     
 
